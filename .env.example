EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
# Python FastAPI backend (Gemini + Browserbase + Redis) — see ../Backend
EXPO_PUBLIC_API_URL=http://localhost:8000
# Sentry — mobile crash/error reporting (same project DSN as backend)
EXPO_PUBLIC_SENTRY_DSN=https://your-key@o4511602668077056.ingest.us.sentry.io/your-project-id
# Optional: only needed if using client-side Google Vision instead of backend /api/identify
# EXPO_PUBLIC_GOOGLE_VISION_API_KEY=your-google-cloud-vision-api-key
# Dev-only: prefill the login form so you don't retype an admin account each reload
# EXPO_PUBLIC_DEV_EMAIL=admin@rrr.test
# EXPO_PUBLIC_DEV_PASSWORD=your-dev-password
